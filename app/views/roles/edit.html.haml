%div{class: "container-fluid mt-3 mb-3 text-center"}
  %h2 Edit role:

  = render 'form', role: @role

%div{class: "container text-center"}
  = link_to 'Show', @role, class: "btn btn-primary"
  |
  = link_to 'Back', roles_path, class: "btn btn-primary"
