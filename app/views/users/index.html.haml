%div{class: "container mt-3 text-center"}
  %h2 Users:
  
%div{class: "container col-6 text-center"}
  %table{class: "table table-bordered table-hover col-6"}
    %thead{class: "table-primary"}
      %tr
        %th{class: "col-3"} Name
        %th{class: "col-6"} Email
        %th{class: "col-2"} Role
        %th{colspan: "3", class: "col-1"} Allowable actions

    %tbody
      - @users.each do |user|
        %tr{style: "vertical-align: middle"}
          %td
            = user.name
          %td
            = user.email
          %td
            = user.role.name
          %td{class: "text-center"}
            = link_to 'Show', user, class: "btn btn-outline-primary"
          %td{class: "text-center"}
            = link_to 'Edit', edit_user_path(user), class: "btn btn-outline-primary"
          %td{class: "text-center"}
            = link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' },
            class: "btn btn-outline-danger"

  %div{class: "container mb-3"}
    = link_to 'New User', new_user_path, class: "btn btn-success"
